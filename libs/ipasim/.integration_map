# Integration mapping for ipasim (iOS simulation)
# Format: upstream_path:goliath_path:action
# Actions: copy, copy+modify, exclude, manual

# This file tracks how upstream ipasim code maps to Goliath's integration.

## Core files (direct copy from upstream)
# src/:libs/ipasim/:copy

## Modified files (copied but with Goliath-specific changes)
# Document any modifications here

## Excluded files (not needed for Goliath integration)
# .github/:exclude
# docs/:exclude
# tests/:exclude
# samples/:exclude

## Manual integration files (managed by Goliath, not synced)
# Makefile.in:manual
# README.md:manual

## Notes
# - ipasim provides iOS application emulation
# - Handles ARM to x86/x64 translation
# - iOS framework emulation
# - Note: ipasim is more of an emulator than translation layer
