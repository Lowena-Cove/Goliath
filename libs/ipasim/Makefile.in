# ipasim stub - complex C++ project, will be fully integrated via upstream sync
# For now, just create an empty static library to allow build to proceed

STATICLIB = libipasim.a

C_SRCS =

# ipasim requires LLVM, Clang, and complex build setup
# Full integration will be handled by upstream sync workflow
